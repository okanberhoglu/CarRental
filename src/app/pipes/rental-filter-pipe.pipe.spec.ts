import { RentalFilterPipePipe } from './rental-filter-pipe.pipe';

describe('RentalFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RentalFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
